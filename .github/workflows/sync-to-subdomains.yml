name: 🔄 Sync PitayaCore → All Subdomains

on:
  push:
    branches: [ main ]
    paths:
      - 'core/**'
      - 'docs/**'
      - '.agent/**'

jobs:
  sync:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        repo:
          - "erp.batidospitaya"
          - "api.batidospitaya"
          - "talento.batidospitaya"

    steps:
      - name: 🚀 Dispatch a ${{ matrix.repo }}
        uses: peter-evans/repository-dispatch@v3
        id: dispatch
        with:
          token: ${{ secrets.SYNC_TOKEN }}
          repository: MiguelGotea/${{ matrix.repo }}
          event-type: pitayacore-sync
          client-payload: |
            {
              "sha": "${{ github.sha }}",
              "triggered_by": "PitayaCore"
            }
      
      - name: 🛰️ Log del Dispatch
        run: |
          echo "🌐 Enviado a: MiguelGotea/${{ matrix.repo }}"
          echo "🆔 SHA enviado: ${{ github.sha }}"
